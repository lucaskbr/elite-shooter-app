import styled from 'styled-components/native';

const EmptyList = styled.Text`
  margin: 20px;
  font-size: 20px;
  text-align: center;
  color: #CACACA;
  font-family: 'Poppins_300Light';
`;

const S = {
  EmptyList
};

export { S };
